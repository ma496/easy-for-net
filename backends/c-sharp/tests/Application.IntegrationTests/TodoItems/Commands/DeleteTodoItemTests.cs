using EasyForNet.Application.Common.Exceptions;
using EasyForNet.Application.TodoItems.Commands.CreateTodoItem;
using EasyForNet.Application.TodoItems.Commands.DeleteTodoItem;
using EasyForNet.Application.TodoLists.Commands.CreateTodoList;
using EasyForNet.Domain.Entities;
using FluentAssertions;

namespace EasyForNet.Application.IntegrationTests.TodoItems.Commands;

using static Testing;

public class DeleteTodoItemTests : BaseTestFixture
{
    [Test]
    public async Task ShouldRequireValidTodoItemId()
    {
        var command = new DeleteTodoItemCommand(99);

        await FluentActions.Invoking(() =>
            SendAsync(command)).Should().ThrowAsync<NotFoundException>();
    }

    [Test]
    public async Task ShouldDeleteTodoItem()
    {
        var listId = await SendAsync(new CreateTodoListCommand
        {
            Title = "New List"
        });

        var itemId = await SendAsync(new CreateTodoItemCommand
        {
            ListId = listId,
            Title = "New Item"
        });

        await SendAsync(new DeleteTodoItemCommand(itemId));

        var item = await FindAsync<TodoItem>(itemId);

        item.Should().BeNull();
    }
}
